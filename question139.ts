/* List three reserved words in JavaScript and create a valid use case for each.*/


let count = 5; // reserved word is let.

if(count > 0) {  // reserved word is if
    console.log("Greater than the 0");
    
}

function add(val1:number,val2: number){
    return val1+val2; // reserved word is return.
}
console.log(add(5,6));
